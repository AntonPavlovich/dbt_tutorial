version: 2

models:
  - name: users
    columns:
      - name: user_id
        tests:
          - not_null
          - unique
      - name: name
        tests:
          - not_null
          - unique    